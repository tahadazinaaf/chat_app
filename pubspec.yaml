name: chat
description: "A new Flutter project."

publish_to: 'none' # Prevent accidental publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^2.24.2  # Latest stable version
  firebase_auth: ^4.15.0  # For authentication (optional)
  cloud_firestore: ^4.17.5  # For database (optional)
  provider: ^6.1.2  # State management (optional)
  firebase_storage: ^11.6.5
  firebase_messaging: ^14.7.10
  image_picker: ^1.1.2
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/image/chat.png